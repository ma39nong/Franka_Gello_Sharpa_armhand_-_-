# 新电脑迁移与环境安装

本工程只保留一种迁移方法：复制完整的 `gello_upper_body_teleop` 文件夹，然后在新电脑
执行根目录的 `./一键安装环境.sh`。

完整说明见：[工程迁移与一键安装说明](../工程迁移与一键安装说明.md)。

不要复制旧 Conda/Pixi 环境，不要使用旧电脑的 ROS `build/install/log`，也不需要预先
导出 Docker 镜像。安装脚本会在目标电脑联网重建这些内容。
