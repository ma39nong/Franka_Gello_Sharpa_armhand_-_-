# 工程迁移与一键安装说明

本文是本工程唯一的电脑迁移流程。工程已经包含 GELLO、DynamixelSDK、Sharpa/MANUS
所需源码和供应商运行库，不需要在原电脑制作迁移包，也不需要导出 Docker 镜像。

迁移流程只有两步：

1. 把完整的 `gello_upper_body_teleop` 文件夹复制到新电脑；
2. 在新电脑执行根目录的 `./一键安装环境.sh`。

安装完成后仍使用原来的三个终端命令：

```bash
./ops/run/run_gello_arms_only.sh
./ops/run/run_sharpa_hands_cyclonedds.sh
./ops/run/start_sharpa_arm_recording_with_tactile.sh --with-cameras
```

## 1. 适用范围

目前支持 x86-64 Ubuntu 22.04/24.04。目标电脑首次安装必须联网，因为安装脚本需要：

- 安装缺失的 Ubuntu、Docker、Miniforge 和 Pixi 组件；
- 创建主机 Conda 和 Sharpa/MANUS Pixi 环境；
- 从 Dockerfile 在线构建 `franka-upper-body-teleop:latest`；
- 重新编译项目 ROS 2 workspace。

Docker 完整构建可能持续较长时间，应在正式采集前完成。安装脚本不会启动机器人、手、
相机或录制器。

`portable_deps/litchi_hardware` 中包含 Sharpa/MANUS 供应商运行库，Orbbec SDK 也受许可
约束。只能把完整工程复制到有权使用这些 SDK 的电脑，不要公开发布。

## 2. 复制完整工程

可以使用移动硬盘、局域网或图形文件管理器复制整个文件夹。命令行示例：

```bash
rsync -a --info=progress2 \
  /home/descfly/ZH/gello_upper_body_teleop/ \
  /media/$USER/<移动硬盘挂载点>/gello_upper_body_teleop/
```

必须保留以下内容：

```text
gello_upper_body_teleop/
├── portable_deps/
│   ├── litchi_hardware/
│   └── gello_software/
├── docker/
├── ops/
├── ros_ws/
├── 一键安装环境.sh
└── 工程迁移与一键安装说明.md
```

当前完整工程约为 575 MB，其中 `portable_deps` 大约为 248 MB。若该目录不存在或明显
过小，不要继续安装。不要只执行 `git clone`：`portable_deps` 含受许可约束的大文件，
故意不纳入 Git。

录制数据不在工程内。若还要迁移历史 bag，应另行复制 `TELEOP_DATA_ROOT`，不要混入工程
目录。

## 3. 新电脑一键安装

把工程复制到新电脑的长期存放位置，不要直接在移动硬盘上安装。然后执行：

```bash
cd /path/to/gello_upper_body_teleop
chmod +x ./一键安装环境.sh
./一键安装环境.sh
```

脚本会提示阅读并接受：

```text
ros_ws/src/orbbec_camera/SDK/End User License Agreement.txt
```

确认接受时输入大写 `YES`。如需非交互安装，可使用：

```bash
./一键安装环境.sh --accept-orbbec-eula
```

默认录制目录为 `$HOME/franka_teleop_data`。需要修改时使用：

```bash
./一键安装环境.sh --data-root /absolute/path/to/franka_teleop_data
```

安装器会完成：

- 安装缺失的系统工具、Docker Engine 和 Compose v2；
- 把当前用户加入 `docker`、`dialout` 用户组；
- 安装 Miniforge 和 Pixi；
- 从 `portable_deps` 安装 GELLO、DynamixelSDK 和 Sharpa/MANUS；
- 创建 Conda/Pixi 环境并重新编译两个 ROS workspace；
- 在新电脑重新构建 Docker 镜像；
- 生成 `docker/.env` 并运行软件、Compose 和 CPU 布局检查。

若第一次运行返回状态码 `3`，表示用户组刚更新。完整注销桌面并重新登录，然后再次执行
同一条命令。

若返回状态码 `4`，表示软件安装完成，但 CPU/IRQ 实时布局不适合启动 FR3。此时不要
使能机械臂，先按第 5 节修改目标电脑配置。

如果目标电脑已有 `docker/.env`，安装器会先保存为
`docker/.env.before-install.<时间>`，不会在不留备份的情况下覆盖。

## 4. 为什么不会影响原电脑现有功能

本次迁移功能没有修改机械臂、手、相机或采集的数据链路：

- `portable_deps` 是与当前运行路径隔离的依赖副本；
- `一键安装环境.sh` 只有被手动执行时才进行安装；
- 原电脑现有 `docker/.env`、硬件参数、ROS topic、QoS 和三个启动命令不变；
- 不执行安装脚本时，新增文件不会启动进程、占用端口或加载设备；
- 安装脚本也只安装软件，不会连接或使能 FR3。

不要在当前已正常工作的原电脑上再次执行 `一键安装环境.sh`。它是给复制后的新电脑使用
的；虽然会备份 `.env`，但没有必要重建当前已经工作的环境。

## 5. 新电脑必须人工核对的硬件配置

软件可以自动安装，但硬件地址与实时 CPU 布局不能从旧电脑盲目照搬。

### FR3 网络、CPU 与 IRQ

以下三处必须按目标电脑一起核对：

1. `docker/.env`
   - `FRANKA_CPUSET`
   - `FRANKA_TELEOP_CPUSET`
   - `HOUSEKEEPING_CPUSET`
2. `config/workcell/current.yaml`
   - 左、右 FR3 的 `robot_ip`；
   - 左、右控制器的 `controller_cpus`。
3. `ops/setup/franka_rt_tuning.sh`
   - `LEFT_DEVICE`、`RIGHT_DEVICE`：连接 FR3 的目标机网卡名；
   - `LEFT_IRQ_CPU`、`RIGHT_IRQ_CPU`：网卡 IRQ 使用的 CPU；
   - `RT_CPUS`：实时 CPU 集合。

查看目标电脑硬件：

```bash
lscpu -e=CPU,CORE,SOCKET,ONLINE
ip -br link
```

修改后运行只读检查：

```bash
./ops/diagnostics/check_franka_cpu_layout.sh
```

还需在 Panda Desk 中解锁两台机器人并启用 FCI，两块 FR3 网卡应分别配置到对应网段。

### GELLO、Sharpa 和相机

- GELLO/OpenRB 序列号：`config/modes/gello.yaml`；
- 三相机序列号和角色：`data_collection/config/cameras.yaml`；
- 网络 cam0 的 IP 和 8090 端口：`docker/compose.yaml` 的 `orbbec-net` 服务；
- FR3 IP 和左右侧映射：`config/workcell/current.yaml`。

连接 GELLO 后先执行：

```bash
conda run --no-capture-output -n gello-upper-body-teleop \
  python ops/diagnostics/check_gello_ports.py --config config/modes/gello.yaml
```

新电脑还需安装并启动与手套匹配的 MANUS Core。三相机采集前关闭 OrbbecViewer，避免
相机被其他进程占用。

## 6. 重要端口在哪里修改

当前三终端主流程的 TCP 端口统一在 `docker/.env` 修改：

| 端口 | 用途 | 配置键 |
| --- | --- | --- |
| TCP 5590 | Operator GUI 与机械臂遥操后端 | `TELEOP_CONTROL_PORT` |
| TCP 5591 | Operator 与 preset IK 服务 | `PRESET_IK_PORT` |
| TCP 5592 | Operator GUI 与常驻采集器 | `COLLECTION_CONTROL_PORT` |

修改后重启对应终端。显式导出的同名环境变量优先于 `docker/.env`。

其他可选链路：

| 端口 | 用途 | 修改位置 |
| --- | --- | --- |
| UDP 5560/5561 | PICO 输入 | `config/modes/pico.yaml` |
| UDP 5570 | 旧 Linker 手部命令 | Linker 配置/启动参数 |
| UDP 5602 | 可选 Wuji telemetry | Wuji 配置/启动参数 |
| TCP 8090 | Orbbec 网络设备端口，不是网页端口 | `docker/compose.yaml` |
| TCP 8080/8081 | 可选手/臂 Web UI | UI 启动参数或 Compose 配置 |

ROS 2 发现域在 `docker/.env` 的 `TELEOP_ROS_DOMAIN_ID` 修改。

## 7. 安装后检查与启动

先执行不会发机器人命令的检查：

```bash
docker compose -f docker/compose.yaml config --quiet
./ops/diagnostics/check_franka_cpu_layout.sh
./ops/diagnostics/check_wuji_gello_environment.sh
```

全部通过后分别打开三个终端。

终端 1：

```bash
cd /path/to/gello_upper_body_teleop
./ops/run/run_gello_arms_only.sh
```

终端 2：

```bash
cd /path/to/gello_upper_body_teleop
./ops/run/run_sharpa_hands_cyclonedds.sh
```

终端 3：

```bash
cd /path/to/gello_upper_body_teleop
./ops/run/start_sharpa_arm_recording_with_tactile.sh --with-cameras
```

首次迁移只录一个 15 至 20 秒测试 episode。确认 `metadata.yaml`、
`collection_state.json`、三路相机、双臂、双手和 20 路触觉全部通过，再开始正式采集。
